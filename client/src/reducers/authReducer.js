/*export default function (state={}, action) {
    switch (action.type) {
        default: 
            return state
    }
}*/

const authReducer = (state={}, action) => {
    switch (action.type) {
        default: 
            return state
    }
}

export default authReducer